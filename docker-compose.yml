version: "3.8"
services:
  api:
    build: ./apps/api
    ports:
      - "3000:3000"
    container_name: extensao-pedro-api
  web:
    build: ./apps/web
    ports:
      - "8081:80"
    container_name: extensao-pedro-web


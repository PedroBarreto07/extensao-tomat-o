# Extensao Pedro — PWA (derived from Chrome extension)

This monorepo contains a PWA derived from the Chrome extension.

Local quick start:
1. Install Docker Desktop and Node.js
2. From repo root: `docker compose build`
3. `docker compose up --build`
4. Open http://localhost:8080

API endpoint: GET /api/quote

Delivered by: PedroBarreto07
